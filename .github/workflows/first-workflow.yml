name: first workflow
on: [push]

jobs:
    run-shell-commands:
        runs-on: ubuntu-latest

        steps:
            - name: echo a string a
              run: echo "Hello World"
            - name: multiline command
              run: |
                node -v
                npm -v